# Solo se usa en el build de produccion (S3/CloudFront), donde el front y el API
# viven en dominios distintos. Ej: https://api.midominio.com
# En dev se deja vacio: Vite proxea /api hacia http://localhost:3000
VITE_API_URL=
